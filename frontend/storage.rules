rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // No storage paths in use yet. Deny all until needed.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
